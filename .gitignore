# Firebase Admin SDK — ห้าม commit คีย์ลับ
config/*firebase-adminsdk*.json

/vendor/
/node_modules/

# ข้อมูลอัปโหลดและ secret ภายในเครื่อง
/uploads/
.env
server/.env

# Local LINE user mapping (optional; may contain identifiers)
line-user-ids.json
